//NEW
//division: (a_f/b_f)
//this function is used to evaluate: (1/b_f)
float reciprocal (float a_f, float b_f){ 

	float z_f; 
	int iterations;
	
	
	//define number of iterations
	//choose randomly 10 iterations. Iterations will be chosen by trial and error
	iterations = 10;
	
	
	
	//define initial value of z_f
	//z_f will take values from a look-up table (array)
	z_f = ;
	
	
	
	
	for (i = 0; i < iterations; i++){
		//use equation for approximating (1/b_f)
		z_f = z_f * (2 - z_f * b_f);

	}



}


//function to create the look-up table (array)
//If precision = w, then the size of the look-up table is (2^w)x(w)
void lookUpTable(){


	w = ;
	
		
	int table[2^w][w]; 

	
	
	
	for(int i=0; i<9; i++)    //This loops on the rows.
	{
		for(int j=0; j<9; j++) //This loops on the columns
		{
			
			
			//assign values to the table
			
			
			
			
		}
	}

	
}
